[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "GOOGLE_CSE_API_KEY,GOOGLE_CSE_ID"

[functions]
  node_bundler = "esbuild"
